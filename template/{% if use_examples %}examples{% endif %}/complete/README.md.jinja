# Complete Example

This example demonstrates how to use the `{{ module_name }}` module.

## Usage

```bash
terraform init
terraform plan
terraform apply
```

## Outputs

The example will output:
- `resource_id` - The ID of the created resource
- `resource_name` - The name of the created resource
- `resource_tags` - The tags applied to the resource
